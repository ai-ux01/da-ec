# AMRYTUM frontend – connect to backend API
# Copy to .env.local and set values.

# Backend API base URL (including /api). Required for Lab Reports and Buy catalog.
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Razorpay – required for payment. When set, "Place order" opens the payment step (Option B).
# Get Key ID from Razorpay Dashboard → API Keys (use test keys for dev).
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxx

# Optional: base URL for Open Graph / link previews (e.g. https://your-app.vercel.app).
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app

# Optional: Sentry DSN for frontend error monitoring (https://sentry.io). Unset = no reporting.
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
